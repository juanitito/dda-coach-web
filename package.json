{
  "name": "dda-coach-web",
  "version": "0.1.0",
  "private": true,
  "description": "BingeDDA — formation DDA assurance",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
